# Miva Smart Attendance Frontend

React frontend for the `backend_cdx` FastAPI attendance backend.

## Setup

```bash
npm install
npm run dev
```

The app expects the backend at `http://127.0.0.1:8000`.

To use another backend URL, create `.env`:

```bash
VITE_API_URL=http://127.0.0.1:8000
```
